services:

  agent:
    build: .
    environment:
      AUTO_APPROVE: "true"
      MAX_ROUNDS: "10"
    env_file:
      - .env
    volumes:
      - ./workspace:/app/workspace
      - ./logs:/app/logs
    restart: on-failure:3
